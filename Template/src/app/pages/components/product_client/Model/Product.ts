import { Local } from "../../local_component/Model/Local";

export class Product {
    id: number;
    name: String;
    cost: number;
    description: String;
    imagen: String;
    amount: number;
    local: Local;
}